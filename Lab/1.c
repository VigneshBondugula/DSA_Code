#include<stdio.h>
int main()
{
    int n,m,k,i,j;
    scanf("%d %d %d",&n,&m,&k);
    int A[n],B[m];
    for(i=0;i<n-1;i++)
    {
        scanf("%d ",&A[i]);
    }scanf("%d",&A[i]);
    for(i=0;i<m-1;i++)
    {
        scanf("%d ",&B[i]);
    }scanf("%d",&B[i]);
}